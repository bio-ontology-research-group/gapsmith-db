name = "MNXref"
upstream_url = "https://www.metanetx.org/mnxdoc/mnxref.html"
download_url = "https://www.metanetx.org/ftp/"
licence = "Creative Commons Attribution 4.0 International (CC-BY-4.0)"
licence_url = "https://www.metanetx.org/mnxdoc/mnxref.html"
attribution = "Moretti S et al. MetaNetX/MNXref: unified namespace for metabolites and biochemical reactions. Nucleic Acids Res. 49(D1):D570-D574 (CC-BY-4.0)"

# Pin policy: MetaNetX release (dot-separated, e.g. "4.5"). Verify at
# https://www.metanetx.org/ftp/ before bumping.
pinned_release = "4.5"
pinned_date = "2025-09-11"
sha256 = ""

artefacts = [
    "chem_xref.tsv",
    "reac_xref.tsv",
    "chem_prop.tsv",
    "reac_prop.tsv",
]

# Per-file pins. The engine prints computed hashes on first fetch; commit them.
[file_hashes]
"chem_xref.tsv" = ""
"reac_xref.tsv" = ""
"chem_prop.tsv" = ""
"reac_prop.tsv" = ""

notes = """
Cross-reference backbone. Used as the second dedup key (after InChIKey) when
merging compounds across ModelSEED, Rhea, ChEBI.

MetaNetX aggregates upstream resources; we use it for namespace mapping only,
never transitively re-redistribute the upstream data it references.
"""
